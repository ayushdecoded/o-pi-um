# o-pi-um

Personal Pi extension package.

## Extensions

- `goal`: durable goal loop with guarded continuation, budgets, status UI, and `/goal_model`.
- `subagent`: tmux-backed child Pi sessions, parallel fan-out, follow-ups by `sessionFile`, model routes, and `/models`.
