{
  "name": "pi-subagents",
  "version": "1.0.0-custom",
  "description": "Minimal Pi extension for spawning normal Pi child sessions as subagents",
  "type": "module",
  "private": true,
  "files": [
    "index.ts",
    "pi-spawn.ts",
    "README.md"
  ],
  "scripts": {
    "check": "pi --no-session --list-models"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
